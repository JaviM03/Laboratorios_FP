/*Crear un programa verifique si el número insertado por el usuario es un elemento mayoritario dentro de un arreglo  unidimensional de 10 valores.
 El elemento mayoritario es aquel que se repite más de n/2 veces, siendo n el tamaño del arreglo */
 #include <stdio.h>

 int main(int argc, char const *argv[])
 {
     /* code */
     return 0;
 }
 